{% extends 'base.html.twig' %}

{% block body %}
    <div>
        <div class="row">
            <div class="col-md-4 offset-md-4 login_form">
                {% for message in app.flashes('notice') %}
                    <div class="alert alert-danger">{{ message }}</div>
                {% endfor %}

                {{ form_start(form) }}
                {{ form_widget(form) }}
                {{ form_end(form) }}
            </div>
        </div>
    </div>
{% endblock %}